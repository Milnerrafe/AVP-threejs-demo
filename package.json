{
  "name": "threejs-morph-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "host": "vite --host",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.1",
    "vite": "^8.0.12",
    "wrangler": "^4.103.0"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
